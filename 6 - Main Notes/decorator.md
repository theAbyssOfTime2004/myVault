2025-11-17 00:03


Tags: [[fsds]], [[high_performance_python]]

# decorator, context manager and high order - function 

- Mục tiêu chính của decorator là để mở rộng chức năng của 1 hàm mà không cần phải chỉnh sửa hay thay đổi hàm đó (trừ một số trường hợp hiếm hoi)
- ![[Pasted image 20251117000636.png]]
=> đi từ trên xuống, *timer* sẽ là lớp decorator ngoài, sẽ là extension của lru_cache, và *lru_cache* sẽ là extension của *hàm predict*
# References
